{
  "name": "luna-moth-gifts",
  "version": "1.0.0",
  "description": "A sleek, modern web application for Luna Moth Gifts, a crystal and gemstone business.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --minify --sourcemap"
  },
  "dependencies": {
    "@google/genai": "^1.21.0",
    "firebase": "^12.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}